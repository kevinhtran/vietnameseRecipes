import React from 'react';
import { Link } from 'gatsby';
import Layout from '../components/Layout';

const Home = () => {
  return (
    <Layout>
      <h1>home page</h1>
    </Layout>
  );
};

export default Home;
